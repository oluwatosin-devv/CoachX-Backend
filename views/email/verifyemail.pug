doctype html
html(lang="en")
  head
    meta(charset="utf-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title CoachX — Verify your email
  body(style="margin:0; padding:0; font-family:'Poppins', Arial, sans-serif; background-color:transparent; color:#222;")
    table(width="100%", border="0", cellspacing="0", cellpadding="0", style="padding:30px 0;")
      tr
        td(align="center")
          table(width="600", border="0", cellspacing="0", cellpadding="0", style="background-color:#ffffff; border-radius:12px; overflow:hidden; box-shadow:0 6px 18px rgba(0,0,0,0.06);")
            // Header
            tr
              td(style="text-align:center; padding:24px 20px; background-color:#fafafa;")
                img(src="https://coach-x.vercel.app/coachX.png", alt="CoachX", style="width:110px; height:auto; display:block; margin:0 auto;")
            // Body
            tr
              td(style="padding:36px 30px; text-align:center;")
                h2(style="margin:0 0 12px 0; font-size:20px; color:#111; font-weight:600;") Verify your email
                p(style="margin:0 0 20px 0; color:#555; line-height:1.6; font-size:15px;")
                  | Hey
                  strong  #{firstName}
                  | , thanks for joining CoachX. Enter the code below to confirm your email and complete sign up.
                // OTP Box
                table(border="0" cellspacing="0" cellpadding="0" align="center" style="margin:18px auto 24px auto;")
                  tr
                    td(style="background:#f5f5f5; padding:18px 22px; border-radius:8px;")
                      p(style="margin:0; font-size:20px; letter-spacing:3px; font-weight:700; color:#111;") #{otp}
                p(style="margin:0 0 20px 0; color:#777; font-size:13px;") This code will expire in
                  strong  10 minutes
                  | . If you didn't request this, simply ignore this email.
                p(style="margin:0 0 22px 0; color:#555; font-size:14px; line-height:1.6;") For security, do not share this code with anyone.
                // Secondary CTA (in case user didn't request)
                p(style="margin:0 0 12px 0;")
                  a(href="#", style="color:#ff8a2b; text-decoration:none; font-weight:600;") Report a problem
            // Footer
            tr
              td(style="text-align:center; padding:16px 24px; font-size:12px; color:#999; background:#ffffff; border-top:1px solid #f0f0f0;")
                | © 2025 CoachX. Built for creators & athletes.
    // Responsive
    style.
      @media only screen and (max-width:600px) {
        table[width="600"] { width: 92% !important; }
        h2 { font-size:18px !important; }
      }
